class PostService {
    public async getPosts() {
        // try {
        //   const foundPosts = await this.prisma.book.findMany()
        //   return foundPosts
        // } catch (error: unknown) {
        //   this.logError(error)
        //   throw error
        // }
      }
}

export default new PostService()