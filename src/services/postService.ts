class PostService {
    public async getPosts() {
        // try {
        //   const foundBooks = await this.prisma.book.findMany()
        //   return foundBooks
        // } catch (error: unknown) {
        //   this.logError(error)
        //   throw error
        // }
      }
}

export default new PostService()